export interface ISwaggerConfig {
    title: string;
    description: string;
    version: string;
}
export declare enum SwaggerConfigEnum {
    TITLE = "title",
    DESCRIPTION = "description",
    VERSION = "version"
}
